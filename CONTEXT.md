# HubX 业务术语表

## 公司业务模式

HubX 是一家**软件外包 + 自运营投放获客**的复合型公司。两条业务线实际上是同一条漏斗：

> 广告投放 → 线索 → 客户 → 合同 → 项目 → 交付 → 利润

- **外包业务**：为甲方客户提供软件定制开发、小程序、APP 等交付服务
- **自运营投放**：在百度、小红书、抖音、淘宝等平台投放广告获取线索，线索转化后签外包合同

## 系统模块全景

### 已有模块

| 模块 | 路由 | 说明 |
|------|------|------|
| 工作台 | `/` | 首页仪表盘 |
| 线索管理 | `/leads/*` | 公海线索、我的线索、垃圾线索、线索详情 |
| 线索成本 | `/lead-cost/*` | 成本看板、投放日报、充值记录、渠道分析 |
| 客户管理 | `/customers/*` | 客户列表、客户详情 |
| 合同管理 | `/contracts/*` | 合同列表、Wizard 新建、编辑、详情 |
| 项目管理 | `/projects/*` | 项目列表、项目详情、交付计划、工作项（需求/任务/缺陷） |
| 日报 | `/dailyreport/*` | 日报列表、日报视图、项目视图 |
| 数据报表 | `/reports` | 销售报表 |
| 财务管理 | `/finance/*` 等 | 财务统计、工资、报价、出差、报销、回款发票 |
| 系统管理 | `/system/*` | 组织架构、权限、公司主体、字典、日志、配置、审批、费用分类 |

### 规划中的扩展与新模块（按优先级）

详细开发计划见 `docs/superpowers/specs/2026-06-30-hubx-development-plan.md`

**第一阶段：基础夯实**
1. **员工管理** — 入离职/转正/合同/考勤/绩效，按职级设定时薪
2. **员工能力建模** — RPG 角色成长系统，五维能力值+技能树+三张画像+招聘量化（规格已定）
3. **个人工作台** — 个人仪表盘 + 任务清单 + 能力面板
4. **日报扩展** — 新增投放日报、通用日报，统一工作种类/项目/工时

**第二阶段：核心业务闭环**
5. **项目成本核算** — 职级时薪 × 日报工时 + 报销 = 项目成本，合同金额 − 成本 = 项目利润
6. **合同回款** — 回款看板优化 + 全合同回款总看板
7. **线索治理** — 跟进达标规则、动作规范、违规机制限制

**第三阶段：运营扩展**
8. **售后运维** — 维护期跟踪、客户工单、续费提醒
9. **供应商管理** — 分包商档案、分包合同、付款记录
10. **会议管理** — 会议安排、会议记录
11. **知识库** — 公司级文档沉淀、SOP、技术方案模板
12. **资产管理** — 服务器/域名/SSL/设备/软件许可证

**第四阶段：智能化**
13. **全链路 ROI** — 投放→线索→客户→合同→项目的完整漏斗分析
14. **AI 驱动** — 合同自动拆解任务、自动分配人员、自动跟进、自动安排会议

## 关键业务概念

### 线索/项目（Lead）
**线索和项目是同一份数据的两个视角，不是两个独立实体。** 线索（Lead）是业务的统一实体，从销售线索到项目交付全流程管理。

**同源双视图：**
- **线索模块**（销售人员使用）：完整生命周期视图——接触客户 → 跟进 → 报价 → 签单 → 合同 → 收款 → 项目执行 → 交付 → 收尾款
- **项目管理模块**（项目经理使用）：项目专注视图——项目启动 → 团队组建 → 需求确认 → 开发 → 测试 → 交付 → 验收 → 收尾
- 底层数据同源，两个模块操作同一份数据，修改双向同步

**签单转换：** 线索签单后，页面不跳转，而是在现有布局上增量增加项目内容（项目概览卡片、项目信息Tab）。线索阶段数据（跟进、报价、出差、报销）保留为只读。

**阶段（Stage）：**
- 线索阶段：待分配 → 待跟进 → 初步建联 → 联系不上 → 需求沟通中 → 方案报价 → 出demo中 → 协商议价
- 转折点：签单后线索状态变为`已签单`，页面自动切换为项目视图
- 项目阶段：未确认 → 未开始 → 进行中 → 已完成 → 验收中 → 搁置/延迟 → 催款中
- 终止：已终止

**归属：** 公海线索（所有人可见）、我的线索（归属人可见）、垃圾线索

**数据权限：**
- 线索阶段数据（跟进记录、报价、出差、报销）：签单前可编辑，签单后只读
- 项目阶段数据（项目信息、交付计划、工作项）：签单后可编辑

**转化规则：**
- 销售在线索详情页操作"签单"，线索状态变为`已签单`
- 系统自动创建关联的项目记录
- 页面自动增量显示项目概览卡片和项目信息Tab

### SABC 分级（工作台占位概念）
- **分级对象统一为「线索」**，不是客户主体。客户主体本身不建 S/A/B/C 级别。
- 由线索的「意向等级 × 预估预算」二维矩阵算出 S/A/B/C 四档，金字塔分布（S 少 / C 多）。
- 包含两个占位字段：`intentLevel`（扩展现有意向等级到 高/中/低/无）、`budgetEstimated`（预估预算，¥）。
- 该分级目前仅用于工作台可视化，是前端占位概念，未接入全系统业务规则；未来接入真实数据时需重新评估分级规则。

### 提醒系统与团队动态的边界
- 个人待办（日报/审批/线索/合同）的单一事实源是 `ReminderContext`，工作台任何「待办计数」「待跟进任务」都从此派生，不再另起待办概念实体。
- 「智能进度提醒」（如「3 天未触达，去破冰」「报价已发 2 天，确认反馈」）与 SABC 分级的「S/A 级超期未触达」流失预警共享同一组占位数据，不重复建模。
- 「团队动态 Feed」（签约/出差/进入阶段/业绩达成）是 HubX 全新占位概念，仅存在于工作台 mock，未接入全系统行为埋点；未来接真实数据时需补一个团队事件流系统（属第四阶段「AI 驱动」范围的子集）。

### 线索推进漏斗（工作台占位概念）
- 漏斗五段：未触达 / 初步沟通 / 跟进中 / 促成阶段 / 已流失。
- 用独立占位字段 `leadStage` 描述，**不与现有线索 `status` 字段共用**。现有页面继续用 `status`，漏斗只读 `leadStage`。
- 「已流失」段 = 已转入垃圾线索池的死透总量；与「S/A 级线索超期未触达」的流失预警（高价值抢救名单）不重叠。

### 线索治理规则
- 跟进效果不达标 → 机制限制（如限制领取新线索数量）
- 跟进动作不规范 → 机制限制（如超时未跟进回收线索）

### 合同（Contract）
- 状态机：草稿 → 审批中 → 待寄出 → 待回寄 → 拟归档 → 已归档 → 已作废
- `拟归档`：合同未回但需要先创建项目时使用
- 从线索/报价单创建，创建后不可修改关联
- 归档或拟归档后，触发线索转为项目

### 项目（Project）
项目是线索（Lead）进入交付阶段后的执行实体，存储项目特有的交付管理数据。

**状态：** 未确认 → 已确认 → 未开始 → 进行中 → 已完成 → 验收中 → 搁置/延迟 → 催款中

**与 Lead 的关系：**
- Lead 是主实体，存储基础信息和跟进历史
- Project 是子实体，存储交付计划、工作项、日报等
- Lead 通过 `projectId` 关联 Project
- Project 通过 `leadId` 回溯 Lead

**触发创建：** Lead 阶段变为`拟归档`或`已签单`时，自动创建未确认的 Project

**确认流程：** 交付部门负责人/项目经理浏览合同、查看线索上下文、安排人员后，确认项目变为正式项目

### 工作项（Work Item）
- 工作项是项目内跟踪工作的单元，分为**需求（Requirement）、任务（Task）、缺陷（Defect）**三种独立模型，不共用统一表
- 工作项通过 `type` 字段区分（`requirement` / `task` / `defect`），但各自有独立字段和状态流
- 工作项编号在项目内递增：`REQ-001`、`TSK-001`、`BUG-001`

### 需求（Requirement）
- 描述一个需要实现的功能或能力
- 状态流：待处理 → 进行中 → 已完成，旁路：已搁置
- 可拆解为多个任务（1:N），但任务也可以独立存在不强制归属需求
- 特有字段：验收标准（acceptanceCriteria）

### 任务（Task）
- 描述一个具体执行的工作单元
- 状态流：待处理 → 进行中 → 已完成，旁路：已阻塞
- 特有字段：预计工时（estimatedHours）、实际工时（actualHours）、检查清单（checklist）
- 检查清单是任务的附属字段（`{ text, done }[]`），不支持独立子任务

### 缺陷（Defect）
- 描述一个需要修复的问题或 Bug
- 状态流：待处理 → 处理中 → 待验证 → 已关闭，旁路：已拒绝、已重开
- 特有字段：严重程度（severity：致命/严重/一般/轻微）、复现步骤（reproductionSteps）
- 缺陷必须带描述，推荐附带截图

### 工作项关联
- 工作项 → 项目：多对一（必填）
- 工作项 → SOP 步骤：多对一（可选），步骤完成时提示联动工作项状态
- 工作项 → 合同：多对一（可选）
- 工作项 → 员工（负责人/创建人）：多对一（必填）
- 需求 → 任务：1:N（可选归属）
- 缺陷 → 任务/需求：可选关联
- 工作项之间支持"关联"（不区分重复/阻塞，未来可通过 type 字段升级）
- 工作项 ↔ 日报：双向关联（日报可勾选工作项，工作项完成后推送日报草稿）

### 工作项评论
- 三种工作项共用一张评论表（通过 workItemType + workItemId 区分）
- 评论支持 @提及，触发提醒

### 工作项操作历史
- 三种工作项共用一张操作历史表
- 记录：创建、状态变更、字段编辑、分配变更
- 结构：action + field + oldValue + newValue + remark

### 日报（Daily Report）
- 按角色区分模板：销售、交付、投放、通用
- 核心字段：工作种类、关联项目、工时
- 日报工时是项目成本核算的输入

### 项目成本核算
- 人力成本 = Σ (人员工时 × 职级标准时薪)
- 项目成本 = 人力成本 + 报销成本
- 项目利润 = 合同金额 − 项目成本
- 职级时薪由财务在人工成本设置中统一维护

### 售后运维
- 项目交付后有免费维护期
- 维护期满后可签付费运维合同
- 客户工单跟踪、续费到期提醒

### 员工能力建模
- 五维能力值（0-100）：技术能力、业务能力、管理能力、工具熟练、领域知识
- 职位权重映射：不同职位对五维能力有不同的权重，加权总分 = Σ(能力值 × 权重)
- L1-L10 等级体系：实习生→初级→中级→高级→专家→架构/总监
- 技能树：最多3层，前置依赖+项目实践触发解锁，允许跨岗位
- 经验三通道：日报驱动（日常）+ 项目结算（阶段性）+ 主管评定（校准）
- 晋级流程：数值达标→解锁资格→评审审批→晋升
- 三张画像：面试画像→入职画像→在职画像，含版本管理
- 招聘量化：面试评分卡（客观测试+主观评分）→ 面试画像 → 初始能力值

### 全链路 ROI
- 广告消耗 → 线索数量/成本 → 客户转化率 → 合同金额 → 项目利润
- 按渠道（百度/小红书/抖音等）拆分 ROI

### 微信群采集（WeChat Group Collection）
通过微信机器人采集业务群聊的全量消息（文本、图片、文件、语音、链接），用于 AI 分析和工作项提取。

**群与线索/项目的关系：** 一对一。一个线索/项目通常只有一个业务群。群名变更时系统自动同步。

**机器人生命周期：** 线索进入"需求沟通中"阶段时，提醒销售将机器人拉入群。机器人加入后不退出，持续采集。

**成员身份识别：** 员工通过微信昵称自动匹配系统用户，客户身份手动确认。映射关系持久化。

**多个机器人账号：** 系统支持多个微信机器人账号（微信号），每个账号可加入多个群。机器人账号在系统设置中统一管理。

### 消息解析层（Message Parsing Layer）
微信群采集的第一层处理。将原始聊天消息提取为结构化的操作、事件、决策。

**处理模式：** 混合模式——工作项提取、满意度、活跃度每天定时批处理；异常预警（客户沉默、负面情绪）实时触发。

**提取粒度：** 需求、任务、缺陷、待办事项、决策/变更、讨论摘要、活跃度指标、满意度信号。

**消息类型：** 全类型采集——文本、图片（OCR）、文件（解析）、语音（ASR）、链接（抓取摘要）。

### 项目上下文（Project Context）
消息解析层的第二层。将提取的操作/事件/决策压缩为项目状态摘要，作为 Agent 的工作上下文。

**结构：** 需求变更记录、已知问题、关键决策、待办事项、客户状态、风险项。

**更新方式：** 每次批处理分析后自动更新，标记为"待确认"或"已确认"。

**展示位置：** 群聊详情页的"项目动态摘要"区域。

### 提取条目（Extracted Item）
AI 从群聊消息中提取的结构化条目，类型包括：需求、任务、缺陷、待办、讨论摘要。

**审核流程：** AI 提取 → 人工审核确认 → 指派给产品经理 → PM 二次指派给执行人 → 写入工作项。

**置信度：** 每条提取结果附带置信度评分，高置信度排在前面，低置信度需要仔细审核。

**来源追溯：** 每条提取结果关联到原始聊天消息，确认入库后在工作项上保留来源链接。

### 预警规则引擎（Alert Rule Engine）
基于群聊消息的异常检测和预警系统。

**规则类型：** 客户沉默（@后未回复）、负面情绪、活跃度下降、关键信息缺失、变更未确认。

**规则进化：** 预置初始规则 → AI 自动学习新模式 → 建议新规则 → 人工审批 → 沉淀入库 → 人工调优。

**通知渠道：** 站内通知（日常）+ 企微机器人（异常预警）+ 短信（紧急情况）。通知发给规则指定的人。

### 客户满意度分析（Customer Satisfaction Analysis）
通过群聊消息的措辞、情绪、行为信号分析客户满意度。

**原则：** 数据忠实反映，不做过度解读。满意度用模糊描述（如"客户近期沟通态度积极"），不用精确评分。

**信号维度：** 措辞（正面/负面词汇）、情绪（感谢/抱怨）、行为（主动确认/已读不回）、变更频率。

### 活跃度分析（Activity Analysis）
通过群聊消息频率分析客户和项目的活跃程度。

**指标：** 日消息量、我方/客户发言比、响应速度、沉默天数、活跃度趋势（本周 vs 上周）。

**展示位置：** 群聊卡片（4行高度）展示活跃度指示 + 今日消息数 + 提取统计。

### 招聘需求（Recruitment Need）
业务部门发起的岗位招聘请求。一个需求可以招多人，系统自动跟踪已到岗/剩余人数。

**审批流程：** 业务部门提交 → 部门负责人一级审批 → HR 确认 → 开始招聘。

**状态：** 待审批 → 招聘中 → 已暂停 → 已关闭 → 已到岗。

### 候选人（Candidate）
应聘者的独立实体。简历独立于招聘需求，一个候选人可同时关联多个需求。每个需求维度有独立的阶段流转和面试记录。

**与需求的关系：** 多对多。候选人可在 A 需求被淘汰后，被 B 需求从人才库重新激活。

### 候选人阶段（Candidate Stage）
按需求维度独立流转的招聘进度。

**阶段模型：** 简历筛选 → 面试(1) → 面试(2) → 面试(N) → 待定薪 → 已发Offer → 已接受 / 已拒绝 / 已淘汰。

**面试轮数：** 不固定，动态创建。每次面试记录为独立条目（轮次、面试官、时间、评价）。

### 人才库（Talent Pool）
所有候选人的统一视图，不管是否关联招聘需求。支持多维度筛选（技能、年限、学历、薪资、来源）。被淘汰的简历保留，可被新需求重新激活。

### 薪资谈判（Salary Negotiation）
包含结构化定薪方案和沟通日志两部分。

**定薪方案：** 基本工资、绩效工资、试用期薪资、试岗期薪资。

**沟通日志：** 每次沟通的时间、渠道、内容摘要、结果。记录完整的薪资谈判过程。

### Offer 管理（Offer Management）
系统根据定薪数据自动生成 Offer，不需电子签。

**状态：** 待生成 → 已生成 → 已发送 → 已查看 → 已接受 / 已拒绝 / 已过期。

### 试岗期（Trial Period）
入职后的前 7 个工作日（可配置），双向考察期。

**评估维度：** 出勤、任务完成、工时消耗、沟通能力、综合评分。

**结果：** 通过 → 进入试用期；未通过 → 淘汰。

### 试用期（Probation Period）
试岗通过后的正式考察期（1-6 个月），区别于试岗期。

**结果：** 转正 → 正式员工；未通过 → 淘汰。

### 面试记录写入员工档案
候选人入职成为员工时，最终入职需求的面试记录写入员工档案（只读快照）。被淘汰的需求不写入。评价 = 结构化打分 + 自由文本备注。
